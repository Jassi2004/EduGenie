{
  "version": 2,
  "builds": [
    {
      "src": "/",
      "use": "@vercel/build-react",
      "config": {
        "buildCommand": "npm run build",
        "outputDirectory": "build"
      }
    }
  ],
  "routes": [
    {
      "src": "/",
      "dest": "/index.html"
    }
  ]
}